<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.indoorway.android.example.fixme">

    <permission
        android:name="com.indoorway.android.example.fixme.fileprovider.READ"
        android:description="@string/permission_to_read_report_images"
        android:label="@string/permission_to_read_report_images" />

    <uses-permission android:name="com.indoorway.android.example.fixme.fileprovider.READ" />

    <permission
        android:name="com.indoorway.android.example.fixme.fileprovider.WRITE"
        android:description="@string/permission_to_read_report_images"
        android:label="@string/permission_to_read_report_images" />

    <uses-permission android:name="com.indoorway.android.example.fixme.fileprovider.WRITE" />

    <application
        android:name=".FixMeApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activities.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.MapActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <!-- Gives access to taken pictures -->
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.indoorway.android.example.fixme"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/shared_file_paths" />
        </provider>

    </application>

</manifest>